%div#header
  - if current_user.present? && current_user.admin?
    = link_to "Add Partners", new_partner_path


  - if signed_in?
    =link_to t(".sign_out"), sign_out_path, method: :delete
  - else
    =link_to t(".sign_in"), sign_in_path

